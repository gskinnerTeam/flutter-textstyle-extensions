name: textstyle_extensions
description: Syntactic sugar for easily modifying TextStyles.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
